import React from 'react';
import { NotFoundMessage } from './styled/not-found';

export const NotFound = () => {
  return <NotFoundMessage>Page Not Found</NotFoundMessage>
};

export default NotFound;